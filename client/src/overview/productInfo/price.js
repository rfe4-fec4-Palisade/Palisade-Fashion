import React, { useState } from 'react';

function Price() {
  return (
    <div>If sale price is null, return original price else return sale price with orignal price line through</div>
  )
}

export default Price;